meta {
  name: Search Models
  type: http
  seq: 7
}

post {
  url: {{rtl433_data_pipeline_url}}/api/v1/models/search
  body: json
  auth: inherit
}

body:json {
  {
    "model": "Tower"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
